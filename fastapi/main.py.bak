from io import BytesIO #[#0] test
import base64

# pip install "fastapi[standard]" # Create and activate a virtual environment and then install FastAPI
from fastapi import FastAPI, Response
#from starlette.responses import FileResponse #[#0] test
# pip install PyPDFForm
from PyPDFForm import FormWrapper#, PdfWrapper 

app = FastAPI()
    
@app.get("/")
async def read_root():
    file = "EECS-GO-Standard-contract_V2.2.pdf"
    
    return {"root": file}


@app.get('/rec_placeholder_b64')
async def get_REC_placeholder_b64():
    file = "EECS-GO-Standard-contract_V2.2.pdf"
    out = base64.encode(file)

    return {"root": out}